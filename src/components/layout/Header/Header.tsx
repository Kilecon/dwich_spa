import styles from './Header.module.scss';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Dwich&Co</h1>
      {/* Menu, logo, etc. */}
    </header>
  );
};

export default Header;